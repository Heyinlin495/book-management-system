# 📱 手机访问图书管理系统详细指南

## 🔧 问题诊断与解决方案

如果您无法通过手机访问图书管理系统，请按照以下步骤逐一排查：

### 1. 检查服务是否正常运行

首先确认后端和前端服务都已经启动：

- 后端服务应运行在 `http://localhost:8080`
- 前端服务应运行在 `http://localhost:5173`

您可以通过在电脑浏览器中访问这些地址来确认服务是否正常运行。

### 2. 获取正确的IP地址

#### 方法一：使用批处理文件（推荐）
双击运行项目目录下的 `get-ip.bat` 文件，将显示您的本机IP地址。

#### 方法二：手动获取
1. 按 `Win + R` 键，输入 `cmd` 打开命令提示符
2. 输入以下命令并回车：
   ```
   ipconfig
   ```
3. 找到您的网络连接对应的IPv4地址，通常是类似 `192.168.x.x` 或 `10.x.x.x` 的格式

### 3. 确保网络连接正常

- 确保手机和电脑连接在同一个Wi-Fi网络下
- 不要使用手机热点给电脑联网，也不要使用电脑热点给手机联网
- 可以在手机上尝试访问电脑的其他服务来测试网络连通性

### 4. 检查Windows防火墙设置

1. 打开"Windows Defender 防火墙"
2. 点击左侧的"高级设置"
3. 在"入站规则"中查找是否有阻止Node.js或Java的规则
4. 如果有，可以尝试暂时禁用防火墙进行测试（测试完毕后请重新启用）

### 5. 确认Vite配置正确

我们已经在 `vite.config.js` 中添加了以下配置：
```javascript
server: {
  host: '0.0.0.0', // 允许外部访问
  port: 5173
}
```

这个配置确保Vite服务可以接受来自外部设备的连接。

### 6. 重启服务

如果以上步骤都确认无误但仍然无法访问，请尝试：

1. 关闭所有服务窗口
2. 双击运行 `start-services.bat` 文件重新启动服务
3. 等待约30秒让服务完全启动
4. 再次尝试在手机上访问

## 🚀 推荐的访问步骤

### 第一步：启动服务
双击运行项目目录下的 `start-services.bat` 文件

### 第二步：获取IP地址
双击运行项目目录下的 `get-ip.bat` 文件，记录显示的IP地址

### 第三步：手机访问
在手机浏览器中输入以下地址：
```
http://[您的IP地址]:5173
```

例如：
```
http://192.168.1.100:5173
```

## 🔐 默认登录账户

系统提供了两个默认账户：

1. **管理员账户**：
   - 用户名：`admin`
   - 密码：`admin123`

2. **普通用户账户**：
   - 用户名：`user1`
   - 密码：`admin123`

> **注意**：登录时需要填写图形验证码，点击验证码图片可刷新。

## ❓ 常见问题解答

### Q: 手机显示"无法访问此网站"怎么办？
A: 请按以下顺序检查：
1. 确认服务已启动且无错误
2. 确认IP地址正确
3. 确认手机和电脑在同一网络
4. 检查Windows防火墙设置

### Q: 能打开登录页面但无法登录怎么办？
A: 请确认：
1. 使用的是默认账户信息
2. 后端服务正常运行
3. 数据库已正确初始化

### Q: 为什么推荐使用批处理文件启动？
A: 批处理文件可以自动完成服务启动和必要的等待时间，避免手动操作可能出现的错误。

## 📞 技术支持

如果按照以上步骤仍然无法解决问题，请提供以下信息以便进一步诊断：

1. 电脑的IP地址
2. 手机和电脑连接的网络名称
3. 访问时浏览器显示的具体错误信息
4. 后端和前端服务启动时的控制台输出信息

---
**祝您使用愉快！** 🎉